function Multiplica(numeroA: number, numeroB: number) {
    numeroA = 5
    numeroB = 8
    let Resultado = numeroA * numeroB
}

const Multiplica2 = (numeroA: number, numeroB: number): number => numeroA * numeroB

// função 2

function Saudacao(Olá: string, nome: string) {
    return Olá + nome
}

console.log (Saudacao)

