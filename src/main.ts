function Multiplica(numeroA: number, numeroB: number): number {
    let Resultado = numeroA * numeroB
    return Resultado
}




const Multiplica2 = (numeroA: number, numeroB: number): number => numeroA * numeroB

// função 2

function Saudacao(nome: string): string {
    return "Olá " + nome  
}  

console.log(Saudacao("Jonas"))  

